# Local mock mode.
APP_ENV=local
DATA_SOURCE_LOCAL=mock
API_BASE_URL_LOCAL=http://localhost:3000
DATABASE_URL=mysql://admin:root@127.0.0.1:3306/37club

# Optional server admin setting.
OWNER_OPEN_ID=

COGNITO_ISSUER=
COGNITO_DOMAIN=
COGNITO_CLIENT_ID=
COGNITO_SCOPES=openid email

# Mock mode does not use server-side S3 credentials.
AWS_PROFILE=
AWS_REGION=ap-northeast-1
S3_BUCKET=
